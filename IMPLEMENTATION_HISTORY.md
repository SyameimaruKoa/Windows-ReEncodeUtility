# 実装履歴

この文書は、このリポジトリの git 履歴を、実装内容が分かる形で 1 コミットずつ追記したものです。要約ではなく、各コミットで実際に何が変わったかを記録しています。

このリポジトリの元になった素材は [git@gist.github.com:85e306167a7f1298d0ec93ea1081c5d3.git](https://gist.github.com/85e306167a7f1298d0ec93ea1081c5d3) 由来で、最初はそのファイル群を AI で最適化するところから始まっている。あわせて、このリポジトリ自体もその AI 最適化を行ったタイミングで git 管理を開始しているため、以後の履歴はその時点からの実装変遷として記録している。

## 2025-05-24

### f7e9b36 - 初期コミット

`ffmpeg` のエンコードオプション用バッチと、再エンコード本体のバッチを追加した。まだ PowerShell 化される前の、CMD ベースの起点となる構成を作成している。

### 3684401 - 文字コードを JIS に変換

バッチファイルの文字コードを Windows の CMD で扱いやすい形に変更した。日本語メッセージの表示崩れを避けるための基礎調整を実施している。

## 2025-06-08

### da6aaa3 - 無駄な装飾を排除

バッチ内の不要な装飾や見栄え重視の出力を整理した。実行時の表示を簡素化し、処理の流れを見やすくした。

### f92e653 - 単体実行時のデバッグを追加

単体で起動したときの挙動を追いやすくするデバッグ手順を追加した。直接実行と呼び出し実行の違いを確認しやすくしている。

### e7193b1 - ややこしい表記を修正

バッチ内の分かりにくい文言や表記ゆれを修正した。操作説明と出力の読みやすさを改善している。

### a2a875d - クソ変更修正

直前の変更で崩れた箇所を戻し、動作を再安定化した。余計な分岐や表示の乱れを取り除いている。

### e01400b - 一旦無理だけどdiff用

差分確認のための作業コミット。機能追加というより、変更点の切り分けを優先した状態だった。

### a538462 - 正常に動くようになった(echo 表示以外は)

再エンコードの主処理が概ね動く状態に復帰した。ただし echo 表示の細部にはまだ課題が残っていた。

### 52a893b - 正常化

主要な処理フローを安定動作する形に整理した。コマンド実行の流れを含め、再エンコードの基盤を整えている。

### 57ff67b - echo内テキストを変数に入れてバグ回避

echo に直接文字列を書く形を避け、変数経由に変更した。表示文字列が原因で壊れる不具合を回避している。

### 4e0448b - だめだめ

実装調整の途中状態。安定化のためにまだ修正が必要な段階だった。

### 4f7ec76 - ゴミ

不要な試行錯誤を含む中間状態。後続修正のための通過点として残された。

### eb8aac3 - 電源処理を変更

終了処理や電源まわりの制御を見直した。エンコード後の振る舞いを調整するための変更を入れている。

## 2025-06-09

### 6094d1e - テンプレートを実装

`templates/テンプレート.template` を導入し、再利用可能なテンプレート構成を追加した。本体バッチとエンコード用バッチをテンプレート経由で扱える土台を作成している。

### 9be4c97 - UTF-8で保存してた

テンプレートの保存文字コードを見直した。PowerShell / 日本語環境で扱いやすい保存形式へ調整している。

### ef55cf2 - フォルダは駄目だった

フォルダ指定では成立しないケースを修正した。テンプレートの扱いをファイルベースへ寄せている。

### 6931dc7 - プリセットを設定できるように改良

エンコードプリセットを選択・設定できるように改良した。オプションを手打ちするだけでなく、選択式で扱える方向に進めている。

### 886150a - exitは駄目だって

処理途中での `exit` 依存を見直した。呼び出し元との関係を壊しにくい終了方式へ寄せている。

### 0b794b4 - デバッグ表示が使えないので抹消

デバッグ表示の一部を削除し、不要な出力を整理した。実運用で邪魔になる表示を減らしている。

### 3b18b09 - QSVのご機嫌取り

Intel QSV まわりの扱いを調整した。ハードウェアエンコードの失敗を減らすための互換性対応を入れている。

### 1761ed4 - QSVのご機嫌取り諦め

QSV 向けの一部対応を見直し、より安定する構成へ修正した。無理に合わせ込むより、壊れにくい選択へ切り替えている。

## 2025-08-19

### af97a3c - 文字コードまわりの調整


`ffmpeg` 用バッチ、テンプレート、本体バッチにまたがる文字コードまわりの調整を実施した。日本語環境での読み込み・表示の安定を再確認している。

### ff710c1 - UTF-8に変更

関連ファイルの保存文字コードを UTF-8 に統一する方向へ変更した。テンプレートと本体の相互運用を意識した整理を行っている。

## 2025-09-23

### ae8913a - 中間ファイル作成

再エンコード途中に中間ファイルを作成する処理を追加した。編集や追加処理を挟みやすい構成へ進化している。

## 2025-10-15

### 0092e57 - FFmpeg encoding options and re-encoding script を追加

`Templates/H264.psd1` と `Templates/vp9.psd1` を追加し、`config.user.psd1`、`get-ffmpegOptions.ps1`、`re-encode-AIOptimus.ps1` を導入した。再エンコードをバッチ中心から PowerShell 中心へ移行する大きな転換点になっている。

### 91b6090 - テンプレートファイルの削除と設定ファイルの更新、fdkaacエンコーダーの追加

テンプレートファイルを整理し、設定ファイル側で管理する構成へ変更した。`fdkaac` エンコーダーを追加し、音声エンコードの選択肢を拡張している。`start.bat` を含む起動経路も更新した。

## 2025-11-29

### 86b8dc2 - Templateのアップデート、追加

`200MB.psd1`、`Discord_2nd.psd1`、`Twitter.psd1` を追加した。用途別テンプレートを増やし、出力先や用途ごとの切り替えをしやすくしている。

### 1bc3d31 - チャプターやSRTファイルによる一括カット機能を仮実装

チャプターや SRT を使った一括カット処理を実装した。単純な再エンコードだけでなく、分割保存の流れを組み込んでいる。

### 382bac3 - 事前チェックを実装

実行前の前提確認を追加した。入力や環境の不足を先に検出して、失敗を減らす構成にしている。

### b6297b7 - ログファイルの扱いを変更し、無駄な出力をしないように抑制

ログファイルの生成と出力方針を見直した。余計な標準出力を減らして、ログへ情報を集約する方向に寄せている。

## 2026-02-17

### ff1a60c - WebM出力オプションを追加し、分割設定を整理

WebM 出力に対応した。分割まわりの設定を整理して、メニュー構成を見直している。

## 2026-02-23

### 7e12c63 - エンコード設定メニューの改善とエラーメッセージの詳細化、バッチファイルにヘルプを追加

`get-ffmpegOptions.ps1` のメニューを改善した。`re-encode-AIOptimus.ps1` のエラーメッセージを詳細化し、`start.bat` にヘルプ導線を追加している。

### d719879 - ログ機能を追加し、エンコード設定や入力ファイル情報をログに記録するように改善

実行ログを本格的に追加した。選択した設定や入力ファイル情報を記録できるようにしている。

## 2026-02-25

### 1ea88d4 - プラットフォームアップロードモードを新規実装

エンコード後に各プラットフォーム向けのアップロードを行うモードを追加した。`200MB.psd1`、`Discord_2nd.psd1`、`Twitter.psd1` と `start.bat` を連携させている。

## 2026-03-01

### 5bb67e9 - Opusビットレート選択肢を拡張し、AV1エンコーダーの詳細設定を追加

Opus のビットレート選択肢を増やした。AV1 の詳細設定を追加して、音声・映像ともに選択肢を強化している。

### 5383333 - AMD HWデコード (d3d11va) の互換性修正と NVIDIA cuda最適化を追加

AMD のハードウェアデコード互換性を修正した。NVIDIA CUDA 側の最適化を追加し、GPU 利用時の安定性を高めている。

### 85cac22 - Vulkanハードウェアデコードの選択肢を追加し、互換性処理を更新

Vulkan ベースのハードウェアデコード選択肢を追加した。既存の互換性処理もあわせて更新している。

### 63401a1 - HW自動検出・音声エンコーダー動的メニュー・2pass対応の大幅強化

ハードウェア自動検出を強化した。利用可能な音声エンコーダーを動的メニュー化し、2-pass エンコード対応を拡張している。

## 2026-03-02

### 7a47600 - コード整理・最適化 + HWアクセル検出/リトライ修正

コード整理と最適化を実施した。ハードウェアアクセラレーションの検出と再試行の流れを修正している。

## 2026-03-13

### 026a4c1 - Add deinterlace options and auto-detect interlaced source

インターレース解除の選択肢を追加した。ソースがインターレースかどうかを自動検出するようにしている。

### e7a73e2 - ffprobe idet fallback を使ってインターレース検出を修正

`ffprobe` と `idet` のフォールバックを使い、ISO / MPEG2 系での判定精度を改善した。自動判定の取りこぼしを減らしている。

### 560b793 - Deinterlace options description を更新

インターレース解除オプションの説明文を更新した。メニューの意味が伝わるように表現を調整している。

### 323db6f - インターレース検出と progressive メニューを改善

インターレース検出ロジックをさらに改善した。progressive 用のメニュー表示も更新している。

### bef7f10 - PowerShell 5.1 compatibility のため UTF-8 BOM を修正

PowerShell 5.1 での互換性を意識し、UTF-8 BOM の扱いを修正した。文字化けや読み込み失敗を減らしている。

### 1312810 - square brackets を含むフォルダでの wildcard path 問題を -LiteralPath で修正

`[]` を含むフォルダ名でワイルドカード展開が起きる不具合を修正した。`-LiteralPath` を使ってパス解釈を安全化している。

### 8c8f644 - nnedi を bwdif に変更し、weights file エラーを解消

`nnedi` 依存のエラーを避けるため、`bwdif` に切り替えた。重みファイルに依存しない構成へ寄せている。

### 941bfac - nnedi3_weights.bin の自動ダウンロードと正しい filter graph を実装

`nnedi3_weights.bin` を自動取得する処理を追加した。`nnedi` 用の filter graph 文字列を正しく組み立てるよう修正している。

### 000be41 - 高品質 IVTC を nnedi + fieldmatch + decimate で追加

`nnedi`、`fieldmatch`、`decimate` を組み合わせた高品質 IVTC を追加した。テレシネ源への対応力を強化している。

### 3377b9e - WMI を使った robust hardware detection を実装

WMI を使ってハードウェア検出を堅牢化した。AMD の誤検出を防ぎ、Intel / NVENC の有無をより正確に判定するようにしている。

### 1678a72 - Split-Path -LiteralPath の bug を修正

PowerShell 5.1 での `Split-Path -LiteralPath` に関する不具合を修正した。スクリプトディレクトリの取得をより安全にしている。

### cfdc5fb - hardware test encodes を復活・強化し、QSV false negatives を改善

直接のハードウェアチェックに戻しつつ、検証用エンコードを強化した。QSV の誤判定を減らし、実際に使えるかどうかを確認しやすくしている。

### 013ceab - Show-Menu に -NoClear を追加

ハードウェアテストログを画面に残すため、`Show-Menu` に `-NoClear` を追加した。選択中の情報を見失いにくくしている。

### ebda9e6 - hardware test と filter fallback のロジックを修正

ハードウェアテストとフィルタのフォールバック処理を再調整した。エラー時でも壊れにくい流れへ修正している。

### 372bcef - QSV と AMF を encoder が見つかった時に保存し、d3d11va ラベルを変更

エンコーダーが見つかったら QSV / AMF 情報を保持するよう修正した。`d3d11va` のラベルをわかりやすく変更している。

### d52a57d - -NoClear 使用時の UI ループ蓄積を防止

`-NoClear` 使用時に画面更新ループが積み上がる不具合を防止した。UI の応答性を維持している。

### 96b2866 - Show-Menu の cursor positioning を安定化

メニューカーソルの位置計算を安定化した。スクロールのズレや表示崩れを抑えている。

### a1fd316 - メニュー項目の padding と background color width を修正

メニュー描画の余白と背景幅を修正した。表示の見栄えと選択しやすさを改善している。

### 1e2f99c - hardware decoders の unsafe fallback を削除

安全でないフォールバック処理を削除した。誤ったデコーダー選択を避ける方向へ整理している。

### 02d71eb - 改行フォーマット

スクリプトの改行フォーマットを整えた。差分の明確化と可読性向上を目的とした整形だった。

## 2026-03-19

### b364260 - 中間ファイルモードに音声エンコード追加と FLAC 対応

中間ファイルモードで音声エンコードも行えるようにした。FLAC にも対応し、編集用中間ファイルの選択肢を増やしている。

## 2026-05-07

### 4799131 - Intel QSV の VP9 設定を -global_quality から -q:v に修正

Intel QSV の VP9 出力で、品質指定を `-q:v` に切り替えた。環境差で効きにくい `-global_quality` の挙動を避けている。

## 2026-05-08

### d08330c - おまかせモードの音声選択を簡素化（自動/無音/コピー）を追加

おまかせモードでの音声選択を簡素化した。自動、無音、コピーの 3 系統に絞って、操作回数を減らしている。

## 2026-05-09

### ae91fa9 - README を完全に改訂・実装履歴を統合

README.md をコード内容に合わせて全面的に拡張・改訂しました。主な変更点:

- ハードウェアエンコーディング対応表（NVIDIA/Intel/AMD/CPU）を追加
- プラットフォーム自動最適化機能（Twitter/Discord/catbox.moe など）の詳細説明を追加
- 依存関係表（必須・オプションツール）を明記
- セットアップ・使い方の手順を詳細化（FFmpeg、config.user.psd1、実行権限）
- 主要機能の詳細説明（インターレース検出、プラットフォーム最適化、外部エンコーダー統合など）を追加
- よくある質問（Q&A）を追加
- ファイル構成図・テンプレート仕様を追加

なお、同日一時的に作成された `MAINTENANCE_LOG.md` の内容は本コミットで `IMPLEMENTATION_HISTORY.md` に統合済みのため、`MAINTENANCE_LOG.md` は削除されました（単一の実装履歴ファイルに統合する方針に合わせた整理です）。

変更はコミット済みです。
